# Linux-File-View
A local-first Linux file manager. Rust backend (Tauri 2) owns all filesystem operations and OS integration; React + TypeScript + Vite + Tailwind frontend renders a dark, terminal-aesthetic UI inside the webview
